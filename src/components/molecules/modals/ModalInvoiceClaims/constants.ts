/** Neutral color for a code that is not in the catalog, or while it is still loading */
export const FALLBACK_STATUS_COLOR = "#6b7280";

/** Shown under any editable cell left empty */
export const REQUIRED_MSG = "* campo obligatorio";

export const DATE_FORMAT = "DD/MM/YYYY";

/** What the API expects for claim_date / response_date */
export const API_DATE_FORMAT = "YYYY-MM-DD";
